# Slack Bot Configuration
# Get these from https://api.slack.com/apps
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
# Optional with Socket Mode (kept for completeness)
# SLACK_SIGNING_SECRET=your-signing-secret-here
SLACK_APP_TOKEN=xapp-your-app-token-here

# Slack user ID to send diagnostic messages to on startup
# Get this from your Slack profile or leave as bot's own user ID
SLACK_TEST_USER_ID=U123456789

# KADI Broker URL (WebSocket)
# Defaults to ws://localhost:8080/kadi if not specified
# Prefer setting this explicitly for clarity
KADI_BROKER_URL=ws://localhost:8080/kadi

# Name of the Slack MCP ability registered on the broker
# If your provider uses a different name, set it here
# Default: Slack MCP Server
# SLACK_ABILITY_NAME=Slack MCP Server

# Optional: Restrict/override which Slack tools are used (comma-separated)
# If unset, a sensible default set is used.
# SLACK_TOOL_WHITELIST=slack_channels_list,slack_conversations_add_message

# OpenAI API Key
# Get this from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here
